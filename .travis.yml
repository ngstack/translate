dist: trusty
sudo: false

language: node_js
node_js:
  - '10'

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

install: yarn

script:
  - yarn spellcheck
  - yarn test translate --watch=false

addons:
  chrome: stable
